import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';

@NgModule({
  imports: [
    RouterModule.forChild([
      /* add entity modules routes here */
    ]),
  ],
})
export class EntityRoutingModule {}
